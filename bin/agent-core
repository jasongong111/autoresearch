#!/usr/bin/env bash
# Gemma 4 skill agent (OpenAI Agents SDK) — chat CLI and web UI.
#
# Usage:
#   export GEMMA4_API_KEY="..."
#   ./bin/agent-core chat "List available skills"
#   ./bin/agent-core chat --interactive
#   ./bin/agent-core serve --port 8766
#
set -euo pipefail

ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${ROOT}:${PYTHONPATH:-}"

ENV_NAME="autoresearch-dashboard"
MAIN_ARGS=(-m agent_core "$@")

if [[ "${CONDA_DEFAULT_ENV:-}" == "${ENV_NAME}" ]] && command -v python >/dev/null 2>&1; then
  exec python "${MAIN_ARGS[@]}"
fi

if command -v conda >/dev/null 2>&1; then
  if conda env list 2>/dev/null | awk '{print $1}' | grep -qx "${ENV_NAME}"; then
    exec conda run --no-capture-output -n "${ENV_NAME}" \
      env PYTHONPATH="${PYTHONPATH}" \
      python "${MAIN_ARGS[@]}"
  fi
fi

PYTHON=""
for candidate in python3.12 python3.11 python3; do
  if command -v "$candidate" >/dev/null 2>&1; then
    PYTHON="$candidate"
    break
  fi
done

if [[ -z "${PYTHON}" ]]; then
  echo "Error: no Python found." >&2
  exit 1
fi

exec "${PYTHON}" "${MAIN_ARGS[@]}"
